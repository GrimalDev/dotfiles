.DS_Store
btop/btop.log
gh/
github-copilot/
nvim/
wireshark/

# Unused app configs — untracked 2026-09-15, still present on disk.
Autodesk/
asciinema/
flutter/
neovide/
ranger/
rofi/
rofi.root/

# tmux plugins are owned by TPM (see tmux.conf @plugin), not by this repo.
# Untracked 2026-09-15: they were recorded as gitlinks with no .gitmodules,
# so a fresh checkout produced empty directories.
tmux/plugins/

# Raycast runtime secrets/state (token, local endpoint token, extensions).
# Never tracked.
raycast/
.omo/
